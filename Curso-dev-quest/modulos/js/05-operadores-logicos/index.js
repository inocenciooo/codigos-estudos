/*
    = atribuir
    == igual o valor
    === idêntico (igual o valor e tipo da variável)
    !== não idêntico
    != diferete
    < menor que
    > maior que
    <= menor ou igual que
    >= maior ou igual que

    && and / e
    || OR / ou

    !(operacao) NOT /  não (inverte o resultado)
    */

const a = 3
const b = 3

console.log(a==b)
